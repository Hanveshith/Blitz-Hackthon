<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="shortcut icon" href="/images/favicon.ico" type="image/x-icon">
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <script src="https://kit.fontawesome.com/ba6d17dfa2.js" crossorigin="anonymous"></script>
  <title>Login</title>
</head>
<body class="bg-blue-200 h-screen flex items-center justify-center">
  <div class="flex flex-col md:flex-row items-center justify-center w-full max-w-screen-xl">
    <div class="mb-8 md:mb-0 md:flex-shrink-0 md:w-1/2 p-8">
      <
    </div>

    <div class="bg-white p-10 rounded shadow-md w-full md:w-1/2">
      <div class="flex items-center justify-center mb-6">
        <i class="fas fa-graduation-cap text-4xl text-blue-500 mr-2"></i>
        <h2 class="text-2xl font-semibold text-blue-500">Seva Login</h2>
      </div>
      <form action="/login" method="post">
        <input name="_csrf" value="<%= csrfToken %>" type="hidden"/>
        <div class="mb-4">
          <label for="mail" class="block text-sm font-medium text-gray-600">User mail<span style="color: red; font-size: large;">*</span></label>
          <div class="relative">
            <span class="absolute inset-y-0 left-0 flex items-center pl-2">
              <i class="fas fa-user text-gray-400"></i>
            </span>
            <input type="email" id="mail" name="mail" class="pl-8 pr-2 py-2 w-full border border-gray-500 rounded-md" required>
          </div>
        </div>
        <div class="mb-4">
          <label for="password" class="block text-sm font-medium text-gray-600">Password<span style="color: red; font-size: large;">*</span></label>
          <div class="relative">
            <span class="absolute inset-y-0 left-0 flex items-center pl-2">
              <i class="fas fa-lock text-gray-400"></i>
            </span>
            <input type="password" id="password" name="password" class="pl-8 pr-2 py-2 w-full border border-gray-500 rounded-md" required>
          </div>
        </div>
        <button type="submit" class="bg-blue-500 text-white p-2 rounded-md w-full">Login</button>
      </form>
      <div style="margin-top: 1rem;text-align: right;">
        <a href="/signup" class="text-blue-500 w-full flex items-center justify-end">
          <span style="font-weight: 500; font-size: medium; color: green;" >Don't have an account? Sign Up <i class="fas fa-arrow-right ml-1"></i> </span>
        </a>
      </div>
      <%- include('partials/flashmessage') %>
      <%- include('partials/flashsuccess') %>
    </div>
  </div>
</div>
</body>
</html>
